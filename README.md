## Extensible Bash Script Runner

To work in dev mode:

  - `./run anything` in terminal

  - Evaluate `break [F8]` to stop command line and signal process

  - Develop `signal` to create your plug-in
