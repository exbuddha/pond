if [[ -e .head ]]; then
    [[ TRACE -eq 0 ]] || {
        mkdir -p "$THREAD/$$/$SIGTS"
        cp -p .head "$THREAD/$$/$SIGTS/$((++HEAD))"
    }
    . .head && rm .head
else
    . .setup
    [[ $SLEEP == */* ]] && SLEEP="$(<$SLEEP)"
    sleep $SLEEP || sleep 1
fi
